<div class="top-wrapper">
  <%= render "shared/top_header" %>
  <div class="top-container">
    <div class="container-text">
    </div>
    <div class="top-search">
      <%= form_with url: search_index_path, class: "search-form", local: true, method: :get do |f| %>
        <%= f.text_field :search, placeholder: "ビール名・キーワードで検索", class: "form-field" %>
        <%= f.submit "検索" %>
      <% end %>
    </div>
  </div>
</div>

<div class="contents-wrapper">
  <div class="container">
    <h3>最新レビュー</h3>
    <div class="review-field">
      <% @reviews.each do |review| %>
        <div class="review-text">
          <p><%= link_to review.beer.name, beer_path(review.beer.id) %></p>
          <%= review.user.nickname %>:<%= review.content %><span><%= link_to "詳細", review_path(review.id) %></span>
        </div>
      <% end %>
    </div>
  </div>
</div>

<div class="registration-wrapper">
  <div class="container">
    <h3>お気に入りのビールを登録しよう</h3>
    <p><%= link_to '登録する', new_beer_path %></p>
  </div>
</div>
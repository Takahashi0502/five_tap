<% if user_signed_in? %>
  <% if comment.gooding_by?(current_user) %>
    <span>いいね済み</span><%= link_to comment.goods.count, comment_good_path(comment.id), method: :delete, remote: true %>
  <% else %>
    <span>いいね</span><%= link_to comment.goods.count, comment_good_path(comment.id), method: :post, remote: true %>
  <% end %>
<% end %>
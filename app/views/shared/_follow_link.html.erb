<% if user.followed_by?(current_user) %>
  <%= link_to 'フォロー中', relationships_path(user_id: user.id), method: :delete, remote: true, class: "followed" %>
<% else %>
  <%= link_to 'フォロー', relationships_path(user_id: user.id), method: :post, remote: true, class: "following" %>
<% end %>